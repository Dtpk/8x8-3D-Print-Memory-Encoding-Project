import sys
import os

LOGO = bytes([
    
])

def fix_header(path):
    if not os.path.exists(path):
        print(f"File not found: {path}")
        return
    
    with open(path, 'rb+') as f:
        data = bytearray(f.read())
        
        # Ensure we have at least 192 bytes
        if len(data) < 192:
            data.extend([0] * (192 - len(data)))
            
        # Nintendo Logo
        data[4:4+156] = LOGO
        
        # Game Title (HUFFMAN)
        title = b"HUFFMAN"
        data[0xA0:0xA0+len(title)] = title
        # Clear rest of title space
        for i in range(0xA0 + len(title), 0xAC):
            data[i] = 0
            
        # Fixed Value (Must be 0x96)
        data[0xB2] = 0x96
        
        # Header Checksum
        checksum = 0
        for i in range(0xA0, 0xBD):
            checksum += data[i]
        checksum = (-(checksum + 0x19)) & 0xFF
        data[0xBD] = checksum
        
        # Padding to 32KB if needed
        # Small ROMs can fail on some hardware if they are not power-of-two padded
        if len(data) < 32768:
            padding_needed = 32768 - len(data)
            data.extend([0] * padding_needed)
            
        f.seek(0)
        f.write(data)
        f.truncate()
        print(f"Success: Fixed header for {path}. New size: {len(data)} bytes.")

if __name__ == "__main__":
    if len(sys.argv) > 1:
        fix_header(sys.argv[1])
    else:
        # Default to local build
        fix_header('huffman_gba.gba')
